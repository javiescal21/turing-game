# Persona Skill

Instructions to generate and maintain a consistent human persona. On first invocation, Claude outputs persona JSON which is saved to `games.claude_persona`. Subsequent calls reload it.
